---
title: The microcontroller bridge
description: What actually happens between your flow and an Arduino — the protocol, the pins, the trade-offs.
---

You drag a Button node onto the canvas, type `2` into a settings field, and a real button on pin 2 starts working. This page explains the layers under that magic — useful context when something does not work the way you expect.

<Clip title="Editing a running flow" duration="0:25" spec="hot-swap">
Each change takes effect immediately. Nothing restarts and the board stays connected.
</Clip>

<img src="/diagrams/microcontroller-bridge.svg" alt="Architecture of the Microflow runtime: React canvas and Rust runtime live on your computer; StandardFirmata and pins live on the Arduino; they talk Firmata over USB; pins connect to the physical world." />

Each layer only knows about the one next to it. The board has no idea what your flow looks like; the canvas has no idea about voltages.

## The Arduino does not know about Microflow

Your flow does not get compiled and uploaded to the board. Instead, Microflow uploads a small generic program once — **StandardFirmata** — and from then on, the desktop app sends short instructions to the board over USB:

- *"Set pin 13 to OUTPUT mode."*
- *"Write HIGH to pin 13."*
- *"Start reporting changes on pin 2."*
- *"Read analog pin A0 every 19 ms."*

These instructions follow the [Firmata protocol](https://github.com/firmata/protocol) — a thin, well-documented standard for talking to microcontrollers. Whatever you build in Microflow ends up as a sequence of Firmata commands.

Running the logic on your computer has three consequences:

### 1. The board is largely interchangeable

While a flow is running over USB, one board is much like another: the flow does not name a board, it names pins. Swap an Uno for a Nano and nothing about the canvas changes.

That stops being true the moment you leave the USB tether. Microflow can also *generate* an Arduino sketch from your flow, and a sketch has to be written for a specific chip — which is why a flow carries a **board target**. Microflow generates code for **Arduino Uno**, **Arduino Nano**, and **ESP32**, and validates your flow against the target's real pin map before it emits anything. See [Choose a board target](/docs/how-to/choose-a-board-target) and [Warnings in the generated sketch](/docs/troubleshooting/sketch-warnings).

### 2. The board is not autonomous

Pull the USB cable and the board stops responding to your flow. The "logic" lives in your computer; the board is a remote-controlled bag of pins. That is the right trade-off while prototyping, because you change behaviour at the speed of dragging a node. It is the wrong trade-off for a finished product.

If you reach the stage where you want a project to run untethered, you do not have to rewrite it. Open **Show code** and Microflow generates the equivalent Arduino sketch, which you can download and upload with the Arduino IDE. The board then runs on its own with Microflow closed. See [Export an Arduino sketch](/docs/how-to/export-an-arduino-sketch).

### 3. The latency model is "fast enough for prototyping"

USB round-trip times are ~1–2 ms on a modern computer. That is far faster than any human can perceive but slower than what you need for tight closed-loop control (motor servoing, audio, anything that needs sub-millisecond response). For UI-feeling behaviour — light up an LED when I press a button, fade based on a knob — it is invisible.

## What runs *inside* Microflow Studio

Studio is built with [Tauri](https://tauri.app), so the same desktop app is part React (UI) and part Rust (everything else). The Rust runtime does the heavy lifting:

- Parses your flow into a graph of components.
- Opens the serial port and speaks Firmata to the board.
- Routes input events from pins to whichever nodes are listening.
- Schedules generators (Interval, Oscillator).
- Runs the [Function](/docs/microflow-studio/nodes/shape/function) node's user-written JavaScript inside an embedded engine — no network round-trip needed.

When you edit a flow on the canvas, the React side serialises the change, the Rust side reconciles it against the running graph, and the board's pin state is updated. You can do this while the flow is running.

## Pin modes

The Firmata protocol defines a pin "mode" — `INPUT`, `OUTPUT`, `INPUT_PULLUP`, `ANALOG`, `PWM`, `SERVO`, etc. Each Microflow node sets its pin to the right mode the moment it starts. This is why:

- **The built-in LED on pin 13 works on any digital pin.** `OUTPUT` needs nothing special.
- **A servo on pin 9 works because pin 9 supports PWM** — try pin 7 (no PWM) and the [Servo](/docs/microflow-studio/nodes/express/servo) node will not be able to drive it smoothly.
- **A button on pin A0 works as a digital input** — analog pins can be used digitally on most boards.

If you change the pin number on a node, the old pin is released and the new pin is acquired in its new mode. This "hot-swap" is fragile: if you have two nodes claiming the same pin, only the most-recently configured one wins. See [Pin conflicts](/docs/troubleshooting/pin-conflicts).

## I²C, SPI, and other buses

I²C is supported: the [I2C Device](/docs/microflow-studio/nodes/sense/i2c-device) node talks to any device by address and register, and the [NFC reader](/docs/microflow-studio/nodes/sense/pn532) node uses it for a PN532. SPI is not currently exposed.

The supported I/O surface:

- Digital pins, read and write.
- Analog input pins.
- PWM-capable digital pins — brightness, intensity, servo.
- Tone output for a piezo.
- AccelStepper — step/direction or H-bridge stepper drivers.
- Addressable LED strips and MAX7219 matrices.
- I²C, by address and register.

## Related

- [Connect a microcontroller](/docs/how-to/connect-a-microcontroller) — the user-facing flow.
- [Pin conflicts](/docs/troubleshooting/pin-conflicts) — what happens when two nodes claim the same pin.
- [Flash firmware manually](/docs/how-to/flash-firmware-manually) — re-uploading StandardFirmata.
- [Export an Arduino sketch](/docs/how-to/export-an-arduino-sketch) — leaving the tether behind.
