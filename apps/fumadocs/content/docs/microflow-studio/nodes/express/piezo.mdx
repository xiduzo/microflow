---
title: Piezo
description: Play tones or compose melodies with a piezo buzzer.
---

A piezo is a small buzzer that can make sounds. It is not a speaker — it produces simple electronic tones rather than rich audio — but it is perfect for beeps, alerts, or simple melodies.

<Clip title="Piezo" duration="0:25" spec="piezo">
Buzz mode gives one tone of the set duration. Song mode plays the sequence at its tempo.
</Clip>

**Example uses:**
- Play a beep when a button is pressed
- Sound an alert when a sensor crosses a threshold
- Play a short melody when a project completes a task

## What you can do

- **Play a tone** — send a frequency value (in Hz) to make the piezo buzz at that pitch. Higher numbers = higher pitch. For example, 440 Hz is the musical note A.
- **Play a melody** — compose a sequence of notes in the node's settings and trigger them to play in order.
- **Stop** — send a signal to stop any sound currently playing.

## Inputs

- **`trigger`** — Play the tone or the song.
- **`stop`** — Stop playing.

## Outputs

- **`value`** — `1` while playing, `0` when silent.

## Settings

- **Type** — Which mode the node runs in. Default `buzz`.
- **Duration** — How long the tone lasts, in milliseconds. Default `500`.
- **Frequency** — The pitch, in hertz. `440` is A above middle C. Default `440`.
- **Pin** — Which pin the component is connected to. Default `11`.
- **Song** — The sequence to play. Default `DEFAULT_SONG`.
- **Tempo** — Beats per minute. Default `113`.

## Resources

- [Johnny-Five — the API this node’s behaviour is modelled on](https://johnny-five.io/api/piezo/)
- [Arduino](https://docs.arduino.cc/built-in-examples/digital/toneMelody/)
