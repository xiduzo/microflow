---
title: Force
description: Detect the amount of force applied to a force-sensitive resistor.
---

A force sensor (also called a Force Sensitive Resistor, or FSR) is a thin, flexible sensor that measures how hard something is pressing on it. The harder you press, the higher the value it outputs.

<Clip title="Force" duration="0:20" spec="force">
The value rises with pressure and falls back when released.
</Clip>

**Example uses:**
- Detect how hard someone is gripping a handle
- Trigger an action when an object is placed on a surface
- Create a pressure-sensitive button that responds differently based on how hard it is pressed

## Output values

The Force node outputs a number that increases as more pressure is applied. The exact range depends on your sensor, but it typically goes from near **0** (no pressure) to a higher value (maximum pressure).

## Inputs

- **`read`** — Take a reading now. The node also reads on its own schedule, so you only need this if you want readings on your terms.

## Outputs

- **`value`** — How hard the sensor is being pressed, `0`–`1023`.

## Settings

- **Pin** — Which pin the component is connected to. Default `A0`.
- **Type** — Which mode the node runs in. Default `analog`.
- **Read frequency** — How often the node reads, in milliseconds. Default `25`.
- **Threshold** — How much the reading must change before the node reports it. Default `1`.

## Resources

- [Johnny-Five — the API this node’s behaviour is modelled on](https://johnny-five.io/api/sensor/)
