---
title: Potentiometer
description: Use a rotary or sliding potentiometer to control values in your flow.
---

A potentiometer is a dial or slider that outputs a value proportional to its position. Think of a volume knob — all the way down gives you a low number, all the way up gives you a high number.

Use it to give yourself real-time manual control over any value in your flow.

**Example uses:**
- Control LED brightness by turning a knob
- Set motor speed with a slider
- Adjust the pitch of a piezo buzzer
- Scrub through LED matrix animation frames

## Output values

The potentiometer outputs a number between **0 and 1023**:
- **0** = one extreme (fully turned or slid one way)
- **1023** = the other extreme (fully turned or slid the other way)

The range is large. Pair the potentiometer with a [Map](/docs/microflow-studio/nodes/shape/map) node to scale the value to something more useful — for example, 0–180 for a servo position, or 0–255 for LED brightness.

## Inputs

- **`read`** — Take a reading now. The node also reads on its own schedule, so you only need this if you want readings on your terms.

## Outputs

- **`value`** — The knob position, `0`–`1023`.

## Settings

- **Pin** — Which pin the component is connected to. Default `A0`.
- **Type** — Which mode the node runs in. Default `analog`.
- **Read frequency** — How often the node reads, in milliseconds. Default `25`.
- **Threshold** — How much the reading must change before the node reports it. Default `1`.

## 2-channel sliding potentiometer

Some linear (sliding) potentiometers have two independent channels — two separate resistive tracks built into one physical slider. This lets you control two values independently with a single component.

<Callout type="info" title="Image placeholder">
  Wiring diagram for a 2-channel linear slider potentiometer, showing how both channels connect to separate analog pins on a microcontroller.
</Callout>

## Related

- Tutorial: [Fade an LED with a potentiometer](/docs/learn/fade-with-potentiometer)
- Tutorial: [Smooth a noisy sensor](/docs/learn/smooth-a-sensor) — applies to potentiometer readings.

## Resources

- [Johnny-Five — the API this node’s behaviour is modelled on](https://johnny-five.io/api/sensor/)
