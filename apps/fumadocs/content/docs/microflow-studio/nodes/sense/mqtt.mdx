---
title: MQTT (Sense)
description: Subscribe to an MQTT topic and feed incoming messages into your flow.
---

The MQTT (Sense) node subscribes to a topic on an MQTT broker — a message-passing server — and emits each message it receives as a value into your flow. It is the subscribe counterpart to the [MQTT (Express)](/docs/microflow-studio/nodes/express/figma) node, which publishes.

<Callout title="Going Figma → hardware? You probably want this node">
When the [Hardware Bridge](/docs/microflow-hardware-bridge) writes a Figma variable change back out to MQTT, this is the node that picks it up in Studio. See [Control hardware from a Figma prototype](/docs/how-to/control-hardware-from-figma).
</Callout>

By default, the node connects to a free public server run by [Mosquitto](https://mosquitto.org/). You can change the connection settings in **Settings → MQTT settings**.

<Callout type="warn" title="Public server means public data">
The default server is open to everyone. Do not send passwords, personal information, or anything sensitive through it. It is fine for testing and prototyping.
</Callout>

## Connection settings

| Setting | What it does |
| --- | --- |
| **Identifier** | A unique name that keeps your messages separate from others using the same server |
| **Host** | The address of the MQTT server |
| **Port** | The connection port number — check your server's documentation |
| **Username** | Only needed if your server requires one |
| **Password** | Only needed if your server requires one |

If you are using the Hardware Bridge, make sure both Microflow Studio and the Figma plugin are configured with the same settings.

## Related

- How-to: [Control hardware from a Figma prototype](/docs/how-to/control-hardware-from-figma) — main use of the Sense MQTT node.
- How-to: [Run your own MQTT broker](/docs/how-to/run-your-own-mqtt-broker)
- Troubleshooting: [MQTT can't connect](/docs/troubleshooting/mqtt-cant-connect)

## Resources

- [mqtt.org](https://mqtt.org/)
