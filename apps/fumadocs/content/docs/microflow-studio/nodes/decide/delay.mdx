---
title: Delay
description: Make your flow wait before continuing.
---

The Delay node holds a signal for a set amount of time before passing it on. Instead of acting immediately when something happens, you can add a pause.

<Clip title="Delay" duration="0:25" spec="delay">
The first press reaches the LED a second late. With Forget previous on, four fast presses produce one event.
</Clip>

**Examples:**
- Wait 2 seconds after a button is pressed before turning on an LED
- Keep a light on for 10 seconds after motion is detected, then let it turn off
- Add a gap between steps in a sequence

## Debouncing

When **debounce** is enabled, the timer resets every time a new signal arrives. The Delay only fires after signals have stopped arriving for the full duration.

Debouncing suits a sensor that fires many times in quick succession. Instead of acting on every reading, you wait for activity to settle down first.

**Example:** A vibration sensor fires 20 times when tapped. With a 500 ms debounce, the Delay waits until tapping has fully stopped for half a second before passing the signal through.

## Inputs

- **`trigger`** — Start the delay.

## Outputs

- **`event`** — Fires once the delay has elapsed.

## Settings

- **Delay** — How long to wait, in milliseconds. Default `1000`.
- **Forget previous** — Restart the wait when a new trigger arrives, instead of queuing it. This is what turns Delay into a debounce. Default `false`.

## Related

- How-to: [Debounce a button](/docs/how-to/debounce-a-button) — uses Delay's debounce mode for clean button events.
