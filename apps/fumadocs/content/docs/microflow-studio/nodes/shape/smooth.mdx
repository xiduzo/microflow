---
title: Smooth
description: Stabilize noisy or jumpy sensor readings by averaging them out over time.
---

Some sensors give readings that jump around even when conditions have not really changed. This is called *noise* — tiny electrical fluctuations that affect the output. The Smooth node removes this jitter by averaging the last several readings into one stable value.

<Clip title="Smooth" duration="0:30" spec="smooth">
The smoothed line flattens as the window grows, and lags further behind the raw one.
</Clip>

**How it works:** The Smooth node keeps a rolling window of recent readings and outputs their average. The larger the window, the smoother — but slower to respond to real changes.

**Example:** A light sensor reads 100, then 105, then 98, then 102 in quick succession, even though the light has not moved. The Smooth node averages these into a stable value around 101, making it much easier to work with.

Smoothing matters most before a [Compare](/docs/microflow-studio/nodes/decide/compare) or [Map](/docs/microflow-studio/nodes/shape/map) node, where erratic values would cause unwanted triggering.

## Inputs

- **`value`** — The noisy value to clean up.

## Outputs

- **`value`** — The smoothed value.

## Settings

- **Type** — Which mode the node runs in. Default `smooth`.
- **Attenuation** — How heavily to smooth, `0`–`1`. Closer to 1 is smoother and slower to react. Default `0.995`.
- **Window size** — How many readings to average. Default `25`.

## Related

- Tutorial: [Smooth a noisy sensor](/docs/learn/smooth-a-sensor) — see noise in action and tune the trade-off.
- How-to: [Debounce a button](/docs/how-to/debounce-a-button) — for digital inputs, use a Delay with debounce instead of Smooth.
