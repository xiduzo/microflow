//! Intel HEX file parser.
//!
//! Defined in `microflow-core` (shared with the browser) and re-exported here
//! so `crate::flasher::hex::parse` keeps working unchanged.

pub use microflow_core::flasher::hex::parse;
