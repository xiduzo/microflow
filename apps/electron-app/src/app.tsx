import { createRoot } from "react-dom/client";
import { ReactFlowCanvas } from "./render/components/react-flow/ReactFlowCanvas";

const root = createRoot(document.body);
root.render(<ReactFlowCanvas />);
