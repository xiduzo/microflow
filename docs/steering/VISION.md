# Microflow Vision

> Generated by `steer-vision`. Refine as the product evolves — do not regenerate from scratch.

Last updated: 2026-09-25

## Product Purpose

Microflow lets people give microcontrollers behavior by drawing a flow on a canvas instead of writing C++. A Firmata-flashed board is the live runtime target; Microflow Studio drives pins, sensors, and protocols in real time over a local serial connection, from the browser (Web Serial) or the desktop app. When a flow is done, Studio can export it as a standalone Arduino sketch so the board runs without a computer. Designers using Figma or Penpot can plug their work into the same flows through an MQTT bridge, and a real-time collaborative canvas lets multiple people prototype together on one board.

## Target Users

| Actor | Context | Primary goals |
| --- | --- | --- |
| Maker / hardware prototyper | Hobby bench, hackerspace, agency prototype | Wire up sensors and actuators and iterate on behavior without firmware code |
| Designer | Figma / Penpot design files driving physical mock-ups | Connect a design artefact to real hardware via MQTT, no firmware engineer needed |
| Educator | Physical-computing classroom or workshop | Teach interaction concepts without spending the first week on Arduino syntax |
| Student | Self-led learning of physical computing | Build a working circuit + behavior on day one; understand by seeing flow execute |

## Core Principles

1. **Low floor, high ceiling** — A beginner with zero hardware knowledge gets a flow running in minutes. A professional building interactive installations gets the depth they need for serious prototypes. Neither group is the secondary citizen.
2. **Local-first, one engine everywhere** — Microflow Studio runs on the maker's machine, in a browser tab or as a desktop app, and talks to the board over the local serial port. Both hosts run the same sans-IO Rust engine (native on desktop, WebAssembly in the browser), so a flow behaves the same on each. The cloud is not a dependency for the core loop.
3. **Visual flow over text code** — The canvas is the program. Dropping into a code editor is never the price of admission for common behavior.
4. **Single source of truth for components** — The Component Catalog (`apps/web/node-components.json`) drives both the UI registry and the Rust runtime registry. UI and runtime cannot drift.
5. **Domain language is enforced** — Code, docs, and conversations use the terms in `CONTEXT.md` (Component, Port, Wiring, BoardHandle, Hardware Callback, …). Generic words like "node" or "handler" are not substitutes.

## Strategic Goals

- [ ] First flow running on a board in under 10 minutes for an absolute beginner with no hardware background
- [ ] Trusted by professional interactive-installation creators for production prototypes
- [ ] Component library covers the common sensor/actuator surface (digital, analog, I2C, stepper, servo) and the common protocols (MQTT, LLM, Firmata)
- [ ] Self-sustaining via Polar subscriptions covering ongoing maintenance

## Bounded Contexts

| Context | Responsibility | Key domain objects |
| --- | --- | --- |
| Flow Authoring | The canvas the maker draws on; ReactFlow nodes; Yjs document of the flow | Flow, Node, Edge, Host Adapter, Component Catalog (UI side) |
| Flow Runtime | Execution of the authored flow in the sans-IO engine (`crates/microflow-core`), hosted natively by the desktop app and as WebAssembly in the browser | FlowRuntime, Component (trait), ComponentRegistry, FlowRouter, Effects, Runtime Host |
| Hardware IO | Single-owner serial connection to the Firmata board (native serial or Web Serial) | BoardHandle, Board IO Loop, BoardConnection, BoardCommand, Hardware Callback |
| Firmware Flashing | Putting Firmata firmware onto a board from either host during bring-up | Board Bring-Up, Flasher |
| Sketch Generation | Exporting a flow as a standalone Arduino sketch (Uno, Nano, ESP32) | Sketch Wiring, board target |
| Design-Tool Bridge | Figma and Penpot plugins that push design state into a flow via MQTT | `@microflow/mqtt`, Microflow Hardware Bridge plugin |
| LLM Integration | Pluggable LLM providers consumed by the `Llm` flow component and by Ask AI, which edits the flow | LLM Provider, Turn Runner, Flow Tool Server |
| Collaboration | Real-time multi-user editing and persistence of flows | Yjs Doc, `@microflow/collab`, collab server |
| Identity & Billing | Accounts and subscriptions | better-auth, Polar, `@microflow/auth` |

## Out of Scope

- Writing raw C++ / Arduino code inside the editor — the flow is the program; an exported sketch is generated from it, and the code view is read-only
- A cloud-hosted runtime — flows run on the maker's machine; the server handles accounts, persistence, and collaboration only
- Live runtimes other than Firmata (CircuitPython, ESP-IDF, bare-metal) — out for now; could expand later

## Open Questions

- Is Microflow a long-term production runtime for shipped installations, or strictly a prototyping tool that hands off to other systems for production? (Not currently in the out-of-scope list.)
- Which paid tier features justify a Polar subscription — collaboration seats, LLM credits, hosted broker, or something else?

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
