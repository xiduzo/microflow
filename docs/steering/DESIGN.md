# Microflow Design Guidelines

> Generated by `steer-design`. Refine as the design system evolves — do not regenerate from scratch.

Last updated: 2026-05-16

## Design Principles

1. **PCB / circuit-board metaphor, end to end.** The interface looks and feels like a real circuit board. Phosphor-green primary, copper-trace edges, soldermask backgrounds; the dark theme is "Aged FR4 / Sepia Substrate", the light theme is "White Soldermask". The metaphor is not decoration — it teaches the domain.
2. **Monospace by default.** IBM Plex Mono (fallback JetBrains Mono / Fira Code) is the only typeface. The product is a tool for makers, not a consumer app.
3. **The canvas is the product.** Chrome stays out of the way. Flow nodes and edges are the protagonists; toolbars, panels, and dialogs are calmer than the canvas.
4. **Direct manipulation over forms.** Drag a node, wire a handle, see the live value glow. Configuration via dialog is a fallback, not the default flow.

## Design System

- **Source of truth:** code. There is no Figma library.
- **Base:** shadcn (`base-lyra` style, `cssVariables: true`, base color `neutral`) plus Tailwind 4. Config at `apps/web/components.json`.
- **Custom layer:** PCB token themes defined in `apps/web/src/index.css` (`:root` for light, `.dark` for dark) using `oklch()` values.
- **Component aliases:** `@/components` (general), `@/components/ui` (shadcn primitives), `@/lib`, `@/hooks`. Icon library: `lucide`. Optional registry: `@magicui`.
- **Other UI libraries in use:** `@base-ui/react` (headless primitives), `leva` (live-tweak controls on flow nodes), `@monaco-editor/react` (in-flow function editor), `vaul` (drawer), `cmdk` (command palette), `sonner` (toasts), `motion` (animations), `react-confetti`, `react-colorful`.

## Tokens

All semantic tokens live in `apps/web/src/index.css`. Always reference the semantic token (`--primary`, `--accent`, `--muted-foreground`), never raw `oklch()` values.

### Color (semantic)

| Token | Light | Dark | Use |
| --- | --- | --- | --- |
| `--background` | `oklch(0.97 0.006 145)` | `oklch(0.17 0.018 55)` | App background — soldermask |
| `--foreground` | `oklch(0.11 0.022 145)` | `oklch(0.85 0.028 55)` | Default text |
| `--primary` | `oklch(0.38 0.16 148)` | `oklch(0.72 0.18 148)` | Phosphor green — primary actions, focus ring, active edges |
| `--accent` | `oklch(0.58 0.14 56)` | `oklch(0.65 0.14 56)` | Copper — selection, trace highlight |
| `--destructive` | `oklch(0.58 0.22 27)` | `oklch(0.60 0.22 25)` | Errors, invalid connections |
| `--muted` / `--muted-foreground` | low-chroma `145` hue | low-chroma `55` hue | Inactive surfaces / text |
| `--border` | `oklch(0.80 0.016 145)` | `oklch(0.35 0.028 55)` | Component borders |
| `--card` / `--popover` / `--sidebar*` | … | … | Surface containers (full set in `index.css`) |
| `--chart-1..5` | green / teal / copper / red / violet | (matched dark) | Charts, monitors |

### Typography

| Token | Value | Use |
| --- | --- | --- |
| `--font-sans` | `"IBM Plex Mono", "JetBrains Mono", "Fira Code", ui-monospace, monospace` | All text (body + UI) — there is no second face |

### Radius

| Token | Value |
| --- | --- |
| `--radius` | `0.25rem` (base) |
| `--radius-sm` … `--radius-4xl` | derived (`+2, +4, +8, +12, +16 px`) |

### Spacing

Use Tailwind's default scale (`gap-1` `gap-2` …). No project-specific spacing tokens.

## Component Patterns

| Pattern | Where | Notes |
| --- | --- | --- |
| Form controls, buttons, dialogs | `apps/web/src/components/ui/` | shadcn primitives — extend, don't fork |
| Flow node | `apps/web/src/components/flow/nodes/<Component>/` | One folder per component; optional `host-adapter.ts` for store wiring |
| Flow handle | `apps/web/src/components/handle.tsx` | Glows copper when selected via edge; green-on-valid / red-on-invalid during connect |
| Flow edge | global CSS in `index.css` (`.react-flow__edges`) | Copper when selected, phosphor-green + drop-shadow + dash animation when animated |
| Live monitor | `leva` panels embedded in nodes | Transparent background; `index.css` overrides Leva chrome |
| In-flow code editor | `@monaco-editor/react` in the `function` node | Highlights template variables via `.template-variable-highlight` |
| Command palette | `cmdk` | Mounted via `react-flow-canvas.tsx` panel layer |
| Toasts | `sonner` | Top-level provider |

## UI States Required

Every interactive element must define: **default / hover / focus / active / disabled**. Additionally:

- **Flow handle** states: idle / connectable / connecting-from / valid-target / invalid-target / selected-via-edge. All six are in `index.css` — do not invent new ones.
- **Flow edge** states: idle / selected / animated / invalid. Animated edges run a `dash-animation` keyframe.
- **Focused inputs in dark mode** get a phosphor glow (`box-shadow: 0 0 10px oklch(0.72 0.18 148 / 0.30)`).

## Accessibility Standards

Baseline: **WCAG 2.1 AA**, plus deliberate choices for low-vision and dyslexia-prone users.

- Minimum contrast: 4.5:1 body text, 3:1 large text / UI components — verify against PCB tokens, not just default shadcn neutrals.
- All interactive elements keyboard-navigable with a visible focus indicator. Phosphor-green focus glow is the project's focus signature; do not suppress it.
- Monospace face (IBM Plex Mono) is part of the accessibility story — it improves character disambiguation for dyslexic readers. Do not introduce a second typeface.
- Color is never the sole means of conveying state. Valid / invalid handle states pair color with handle text and cursor changes.
- Form inputs always paired with a `<label>` (not placeholder text).
- The flow canvas keyboard support is a **known weak spot**; new interactions on the canvas must be checked for keyboard parity before merge.

## Responsive Behavior

- **Microflow Studio (`apps/web`, Tauri)** is desktop-primary. Target ≥ 1024px Tauri window; degrade gracefully down to ~900px wide. Touch and mobile are not targets.
- **Docs site (`apps/fumadocs`)** is fully responsive — mobile, tablet, desktop.
- **Marketing / landing surfaces** follow the docs site rules.

| Breakpoint | Width | Surfaces affected | Behavior |
| --- | --- | --- | --- |
| Mobile | < 768px | Docs only | Single column; collapsed nav |
| Tablet | 768–1024px | Docs only | Two-pane where useful |
| Desktop | ≥ 1024px | Studio + Docs | Full layout |

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
